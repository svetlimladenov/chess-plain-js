import React, { Component, Fragment } from "react";

class Controls extends Component {
  render() {
    return (
      <Fragment>
        <button>Start</button>
        <button>Reset</button>
      </Fragment>
    );
  }
}

export default Controls;
